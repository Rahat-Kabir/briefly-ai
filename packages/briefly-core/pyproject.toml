[project]
name = "briefly-core"
version = "0.1.4"
description = "Core library for Briefly."
requires-python = ">=3.11"
dependencies = [
  "litellm>=1.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
